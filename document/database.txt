
**المستخدمون المستهدفون:**
```
1. الزائر (Guest)
   - تصفح الباقات
   - استخدام الفلترة
   - التسجيل في المنصة

2. المستخدم المسجل (User)
   - كل صلاحيات الزائر
   - استخدام الشات الذكي
   - حجز رحلات (جاهزة + مخصصة)
   - إدارة الحجوزات
   - الدفع الإلكتروني

3. المدير (Admin)
   - كل صلاحيات المستخدم
   - إدارة الباقات والخدمات
   - إدارة الحجوزات
   - إدارة المستخدمين
   - التقارير والإحصائيات
```

### **2. تحليل حالات الاستخدام (Use Cases)**

#### **المجموعة أ: إدارة الباقات الجاهزة**
```
UC-01: تصفح الباقات
UC-02: البحث والفلترة المتقدمة
UC-03: عرض تفاصيل الباقة
UC-04: الحجز المباشر
```

#### **المجموعة ب: نظام الذكاء الاصطناعي**
```
UC-05: بدء محادثة جديدة
UC-06: إدخال المتطلبات (ميزانية، وقت، تفضيلات)
UC-07: عرض الخطة المقترحة
UC-08: تعديل الخطة
UC-09: تأكيد الخطة النهائية
```

#### **المجموعة ج: نظام الحجز والدفع**
```
UC-10: إنشاء حجز جديد
UC-11: إدارة حالات الحجز
UC-12: معالجة الدفع
UC-13: تأكيد الحجز
```

### **3. تحليل سير العمل (Workflow Analysis)**

#### **المسار الأول: الباقات الجاهزة**
```
1. الدخول → 2. التصفح → 3. الفلترة → 4. اختيار الباقة → 
5. عرض السعر → 6. التأكيد → 7. الدفع → 8. التأكيد النهائي
```

#### **المسار الثاني: الرحلات المخصصة**
```
1. الدخول → 2. بدء المحادثة → 3. جمع المتطلبات → 
4. توليد الخطة → 5. التعديل → 6. التسعير → 
7. التأكيد → 8. الدفع → 9. التأكيد النهائي
```

### **4. تحليل البيانات المطلوبة**

#### **بيانات الإدخال:**
- بيانات المستخدم الشخصية
- تفضيلات السفر (من الشات)
- معايير البحث والفلترة
- بيانات الحجز والدفع

#### **بيانات الإخراج:**
- قوالب الباقات
- خطط الرحلات المخصصة
- أسعار وتفاصيل الحجز
- تأكيدات ووصلات الدفع

### **5. تحليل القيود والشروط (Constraints)**

#### **قيود الأعمال:**
- نظام التسعير المزدوج (تقريبي/نهائي)
- سياسة الإلغاء والتعديل
- شروط وقيود كل باقة

#### **قيود تقنية:**
- أداء نظام الفلترة
- استجابة الذكاء الاصطناعي
- أمان بيانات الدفع
- تكامل واجهات APIs

---

## 🗃️ **الانتقال للمهمة 2: تصميم هيكل قواعد البيانات**

### **النماذج الأساسية المطلوبة:**

#### **1. نموذج المستخدم (User)**
```python
# المخطط المقترح
User:
  - id
  - email
  - password
  - full_name
  - phone
  - role (user/admin)
  - created_at
  - updated_at
```

#### **2. نموذج الباقة (Package)**
```python
Package:
  - id
  - title
  - description
  - duration_days
  - price
  - destinations (M2M)
  - services (M2M)
  - daily_schedule (JSON)
  - is_active
```

#### **3. نموذج الخدمة (Service)**
```python
Service:
  - id
  - name
  - type (hotel/transport/restaurant/activity)
  - description
  - location
  - price_range
```

#### **4. نموذج الحجز (Booking)**
```python
Booking:
  - id
  - user (FK)
  - package (FK) / custom_trip (FK)
  - booking_type (package/custom)
  - status (pending/confirmed/cancelled)
  - total_price
  - booking_date
```

#### **5. نموذج المحادثة (ChatSession)**
```python
ChatSession:
  - id
  - user (FK)
  - session_data (JSON)
  - requirements (JSON)
  - generated_plan (JSON)
  - status (active/completed)
```
